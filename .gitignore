
# macOS junk
.DS_Store

# JetBrains IDE
.idea/
.env

# Widgets (Node/bun) — deps and local build output
widgets/node_modules/
widgets/dist/

# Generated widget builds embedded by core. Keep the placeholder manifest.json
# (committed so //go:embed widgets compiles before the first build); ignore the
# content-hashed HTML the vite build emits.
core/mcp/ui/widgets/*.html

/data
/databases.example.yml
/docker-compose.example.yml
